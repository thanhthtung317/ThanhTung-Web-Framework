{
  "devDependencies": {
    "buffer": "^5.7.1",
    "json-server": "^0.17.1"
  },
  "dependencies": {
    "axios": "^1.2.0",
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "npx parcel index.html",
    "start": "concurrently npm:start:*"
  }
}
